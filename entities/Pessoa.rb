class Pessoa
  def initialize(first_name, last_name, address, city)
    @first_name = first_name
    @last_name = last_name
    @address = address
    @city = city

    # puts "FirstName: #{@first_name}"
    # puts "LastName: #{@last_name}"
    # puts "address: #{@address}"
    # puts "city: #{@city}"
  end
end
